import React from 'react';
declare type Props = {
    children: React.ReactElement;
    id: string;
    pathname?: string;
};
export default function SharedElement({ children, id, pathname }: Props): JSX.Element;
export {};
